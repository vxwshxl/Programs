# Experiment 7: Library Joins, Union, and Referential Integrity

## Aim
To perform advanced join operations (`INNER JOIN`, `LEFT JOIN`), combine datasets using the `UNION` operator, and demonstrate the purpose of foreign key constraints on data integrity in MySQL.

---

## 1. Table Setup

Copy and run this block in your MySQL client to create the tables and insert the sample data.

```sql
CREATE TABLE Categories (
    category_id INT PRIMARY KEY,
    category_name VARCHAR(100) NOT NULL
);

CREATE TABLE Books (
    book_id INT PRIMARY KEY,
    title VARCHAR(150) NOT NULL,
    author VARCHAR(100) NOT NULL,
    category_id INT,
    FOREIGN KEY (category_id) REFERENCES Categories(category_id) ON DELETE CASCADE
);

CREATE TABLE Borrowed_Books (
    borrow_id INT PRIMARY KEY,
    book_id INT,
    member_name VARCHAR(100) NOT NULL,
    borrow_date DATE NOT NULL,
    FOREIGN KEY (book_id) REFERENCES Books(book_id) ON DELETE CASCADE
);

INSERT INTO Categories (category_id, category_name) VALUES
(1, 'Science'),
(2, 'Fiction'),
(3, 'History'),
(4, 'Biography');

INSERT INTO Books (book_id, title, author, category_id) VALUES
(101, 'A Brief History of Time', 'Stephen Hawking', 1),
(102, 'The Grand Design', 'Stephen Hawking', 1),
(103, 'To Kill a Mockingbird', 'Harper Lee', 2),
(104, 'Sapiens', 'Yuval Noah Harari', 3),
(105, 'Steve Jobs', 'Walter Isaacson', 4),
(106, 'The Origin of Species', 'Charles Darwin', 1);

INSERT INTO Borrowed_Books (borrow_id, book_id, member_name, borrow_date) VALUES
(501, 101, 'John', '2026-05-01'),
(502, 103, 'Jane', '2026-05-02'),
(503, 104, 'John', '2026-05-03'),
(504, 105, 'Robert', '2026-05-05');
```

---

## 2. Queries and Solutions

### i) Write an SQL query to fetch the list of books borrowed by members, along with their corresponding category names. Use the INNER JOIN operation on the Books, Categories and Borrowed_Books tables.

#### SQL Query:
```sql
SELECT 
    b.title, 
    b.author, 
    c.category_name, 
    bb.member_name
FROM Borrowed_Books bb
INNER JOIN Books b ON bb.book_id = b.book_id
INNER JOIN Categories c ON b.category_id = c.category_id;
```

#### Output:
| title | author | category_name | member_name |
|---|---|---|---|
| A Brief History of Time | Stephen Hawking | Science | John |
| To Kill a Mockingbird | Harper Lee | Fiction | Jane |
| Sapiens | Yuval Noah Harari | History | John |
| Steve Jobs | Walter Isaacson | Biography | Robert |

#### Explanation:
* An `INNER JOIN` finds matching records in both tables.
* By joining `Borrowed_Books` to `Books` (using `book_id`) and then to `Categories` (using `category_id`), we retrieve borrowing details, including titles and categories.

---

### ii) Write an SQL query to find all the books that are not currently borrowed by any member. Use the LEFT JOIN operation on the Books and Borrowed_Books tables.

#### SQL Query:
```sql
SELECT 
    b.book_id, 
    b.title, 
    b.author
FROM Books b
LEFT JOIN Borrowed_Books bb ON b.book_id = bb.book_id
WHERE bb.book_id IS NULL;
```

#### Output:
| book_id | title | author |
|---|---|---|
| 102 | The Grand Design | Stephen Hawking |
| 106 | The Origin of Species | Charles Darwin |

#### Explanation:
* A `LEFT JOIN` returns all records from the left table (`Books`) and matching rows from the right table (`Borrowed_Books`).
* If there is no match on the right side, the columns from `Borrowed_Books` display as `NULL`.
* `WHERE bb.book_id IS NULL` filters the output to only display these unborrowed books.

---

### iii) Write an SQL query to combine the following two datasets using the UNION operator:
* The names of all authors of books in the Science category.
* The names of all authors of books borrowed by the members with the name John.

#### SQL Query:
```sql
SELECT b.author
FROM Books b
JOIN Categories c ON b.category_id = c.category_id
WHERE c.category_name = 'Science'

UNION

SELECT b.author
FROM Books b
JOIN Borrowed_Books bb ON b.book_id = bb.book_id
WHERE bb.member_name = 'John';
```

#### Output:
| author |
|---|
| Stephen Hawking |
| Charles Darwin |
| Yuval Noah Harari |

#### Explanation:
* The first query gets authors of books in the `'Science'` category.
* The second query gets authors of books borrowed by `'John'`.
* The `UNION` operator combines these two datasets and removes any duplicate values.

---

### iv) Assume you are adding a new book to the Books table. Write one SQL statement to insert the record, ensuring that the category_id of the book matches an existing category_id in the Categories table. Explain how foreign key constraints ensure data integrity in this scenario.

#### SQL Statement:
```sql
INSERT INTO Books (book_id, title, author, category_id) VALUES (107, 'Cosmos', 'Carl Sagan', 1);
```

#### Verification Query:
```sql
SELECT * FROM Books WHERE book_id = 107;
```

#### Output:
| book_id | title | author | category_id |
|---|---|---|---|
| 107 | Cosmos | Carl Sagan | 1 |

#### Explanation of Foreign Keys and Data Integrity:
* A **Foreign Key (FK)** constraint creates a link between two tables, specifically referencing the primary key of another table (e.g., `category_id` in `Books` points to `category_id` in `Categories`).
* If you attempt to insert a record containing an invalid key (e.g., `category_id = 99`):
  ```sql
  INSERT INTO Books (book_id, title, author, category_id) VALUES (108, 'Test Book', 'Test Author', 99);
  ```
  MySQL will block this insert and display an error:
  `ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails...`
* This protects **Referential Integrity** by ensuring that a book cannot be assigned to a category that does not exist.
