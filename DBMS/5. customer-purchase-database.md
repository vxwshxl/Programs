# Experiment 5: Customer Purchase Database

## Aim
To design a database schema for customer purchases using `Customer`, `Item`, and `Purchase` tables with foreign key constraints, and query data for purchase aggregates, monthly filtering, and join checks.

---

## 1. Table Setup

Copy and run this block in your MySQL client to create the tables and insert the sample data.

```sql
CREATE TABLE Customer (
    c_id INT PRIMARY KEY,
    c_name VARCHAR(100) NOT NULL,
    address VARCHAR(200) NOT NULL
);

CREATE TABLE Item (
    i_code INT PRIMARY KEY,
    i_name VARCHAR(100) NOT NULL,
    price DECIMAL(10, 2) NOT NULL
);

CREATE TABLE Purchase (
    p_id INT PRIMARY KEY,
    c_id INT,
    i_code INT,
    qty INT NOT NULL,
    date DATE NOT NULL,
    FOREIGN KEY (c_id) REFERENCES Customer(c_id) ON DELETE CASCADE,
    FOREIGN KEY (i_code) REFERENCES Item(i_code) ON DELETE CASCADE
);

INSERT INTO Customer (c_id, c_name, address) VALUES
(1, 'Aditya Baruah', 'Guwahati'),
(2, 'Barnali Sen', 'Jorhat'),
(3, 'Chayanika Devi', 'Tezpur'),
(4, 'Deepankar Roy', 'Silchar');

INSERT INTO Item (i_code, i_name, price) VALUES
(101, 'Wireless Mouse', 800.00),
(102, 'Mechanical Keyboard', 2500.00),
(103, 'Gaming Headset', 1800.00),
(104, 'LED Monitor', 12000.00);

INSERT INTO Purchase (p_id, c_id, i_code, qty, date) VALUES
(501, 1, 101, 3, '2026-02-10'),
(502, 2, 102, 2, '2026-02-14'),
(503, 1, 102, 1, '2026-02-18'),
(504, 3, 104, 1, '2026-01-20'),
(505, 2, 104, 1, '2026-03-05');
```

---

## 2. Queries and Solutions

### a) Find the name of the customer who has done the maximum purchase (in terms of total money spent).

#### SQL Query:
```sql
SELECT c.c_name, SUM(p.qty * i.price) AS total_spent
FROM Customer c
JOIN Purchase p ON c.c_id = p.c_id
JOIN Item i ON p.i_code = i.i_code
GROUP BY c.c_id, c.c_name
ORDER BY total_spent DESC
LIMIT 1;
```

#### Output:
| c_name | total_spent |
|---|---|
| Barnali Sen | 17000.00 |

#### Explanation:
* We join the tables to match customer names with their quantities purchased and item prices.
* `SUM(p.qty * i.price)` calculates the total spending for each customer.
* `ORDER BY total_spent DESC LIMIT 1` sorts customers descending by spending and retains only the single top spender.

---

### b) Display the name of the item that has been purchased maximum number of times (maximum sum of quantity) in the month of February.

#### SQL Query:
```sql
SELECT i.i_name, SUM(p.qty) AS total_qty_purchased
FROM Item i
JOIN Purchase p ON i.i_code = p.i_code
WHERE MONTH(p.date) = 2
GROUP BY i.i_code, i.i_name
ORDER BY total_qty_purchased DESC
LIMIT 1;
```

#### Output:
| i_name | total_qty_purchased |
|---|---|
| Wireless Mouse | 3 |

#### Explanation:
* `WHERE MONTH(p.date) = 2` filters transaction records to show only February purchases.
* `SUM(p.qty)` sums up the total units sold for each item.
* `ORDER BY total_qty_purchased DESC LIMIT 1` retrieves the item with the highest quantity sold in February.

---

### c) Display the name of the customer who did not purchase any item.

#### SQL Query:
```sql
SELECT c_name 
FROM Customer 
WHERE c_id NOT IN (
    SELECT DISTINCT c_id 
    FROM Purchase
);
```

#### Output:
| c_name |
|---|
| Deepankar Roy |

#### Explanation:
* The subquery `SELECT DISTINCT c_id FROM Purchase` compiles a list of IDs of customers who have completed at least one transaction.
* The outer query finds customers whose `c_id` is not present in that list, identifying who made no purchases.
